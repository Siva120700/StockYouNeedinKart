# Local dev (Vite proxies /graphql → http://localhost:5080)
VITE_GRAPHQL_URL=/graphql

# Vercel production: set this in Vercel → Project → Settings → Environment Variables
# to your public tunnel URL, e.g.:
# VITE_GRAPHQL_URL=https://xxxx.trycloudflare.com/graphql
